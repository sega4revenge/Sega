<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="@dimen/dist_medium"
    android:background="@color/text_secondary"
    android:orientation="vertical"
    android:padding="@dimen/dist_large">

    <!-- Header -->
    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal">

        <ImageView
            style="@style/MovieItemIconsStyle"
            android:src="@drawable/icon_overview"
            tools:ignore="ContentDescription" />
<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
        <com.sega.vimarket.widget.RobotoBoldTextView
            style="@style/MovieItemHeaderStyle"
            android:text="@string/detail_header_overview" />
        <ImageButton
            android:layout_width="25dp"
            android:layout_height="25dp"
            android:id="@+id/edit_productoverview"
            android:background="@drawable/edit2"
            android:layout_alignParentTop="true"
            android:layout_alignParentEnd="true"
            android:layout_gravity="right"/>
</RelativeLayout>
    </LinearLayout>

    <!-- Value -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">
<RelativeLayout
    android:layout_width="match_parent"
    android:layout_height="wrap_content">
    <com.sega.vimarket.widget.RobotoLightTextView
        android:id="@+id/product_overview_value"
        style="@style/MovieItemValueStyle"
        android:layout_width="match_parent"
        tools:text="My movie overview goes here" />

</RelativeLayout>
    </LinearLayout>

</LinearLayout>